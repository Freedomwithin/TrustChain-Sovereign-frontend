# Backend URL for TrustChain API
# Example: https://trustchain-backend.vercel.app or http://localhost:3000
VITE_BACKEND_URL=https://trustchain-backend.vercel.app

# Solana RPC URL (Required for notary service)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# TrustChain Program ID (Required for notary service)
# This should match the deployed program ID
TRUSTCHAIN_PROGRAM_ID=YourProgramIDHere

# Notary Secret (Required for notary service - Server Side Only)
# A JSON array of the secret key, e.g. [1, 2, 3, ...]
NOTARY_SECRET=[]

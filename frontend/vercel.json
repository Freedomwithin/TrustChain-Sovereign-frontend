{
  "framework": "vite",
  "buildCommand": "yarn workspace trustchain-frontend build",
  "outputDirectory": "dist",
  "devCommand": "yarn workspace trustchain-frontend dev",
  "installCommand": "yarn install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}